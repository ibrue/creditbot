# Test dependencies: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
httpx>=0.27.0   # required by fastapi.testclient
pytest-cov>=5.0.0   # optional: pytest --cov=. --cov-report=term-missing
